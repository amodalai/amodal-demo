{
  "description": "Load the five demo submissions (with their documents and claims) into the stores. Idempotent: submissions that are already present are skipped, so it is safe to run again. The offline shortcut when no broker mailbox is connected; the Sync inbox button is the primary path.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "runningLabel": "Seeding the demo submissions",
  "completedLabel": "Seeded the demo submissions",
  "uses": {
    "tools": [
      "store__submissions__query",
      "store__submissions__set",
      "store__documents__set",
      "store__claims__set"
    ]
  },
  "triggers": [
    {
      "kind": "regex",
      "pattern": "^\\s*seed(?:\\s+examples)?\\s*$",
      "flags": "i",
      "continueToLLM": true
    }
  ]
}
