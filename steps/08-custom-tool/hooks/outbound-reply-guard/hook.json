{
  "name": "outbound-reply-guard",
  "label": "Outbound reply guard",
  "description": "Checks model-selected send_message calls and blocks recipients whose known submissions have no saved finding. The send_outcome handler checks its own saved finding and recipient; its nested send does not enter preToolUse.",
  "points": ["preToolUse"],
  "capabilities": ["store:read", "reads_tool_io", "gates_tools"],
  "failPolicy": "closed",
  "config": {
    "guardedTools": ["send_message"]
  }
}
