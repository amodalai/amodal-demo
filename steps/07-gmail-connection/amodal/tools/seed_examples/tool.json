{
  "description": "Load the four demo submissions (with their documents and claims) into the stores. Idempotent: submissions that are already present are skipped, so it is safe to run again. Runs from the `seed` chat command and from the UI on first open, when the submissions store is empty.",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "runningLabel": "Seeding the demo submissions",
  "completedLabel": "Seeded the demo submissions",
  "timeout": 60000,
  "execution": "durable",
  "uses": {
    "tools": [
      "store__submissions__query",
      "store__submissions__set",
      "store__documents__set",
      "store__claims__set"
    ]
  },
  "triggers": [
    { "kind": "invoke" },
    {
      "kind": "regex",
      "pattern": "^\\s*seed(?:\\s+examples)?\\s*$",
      "flags": "i",
      "continueToLLM": true
    }
  ]
}
