{
  "name": "claims",
  "key": "{claim_id}",
  "schema": {
    "claim_id": { "type": "string" },
    "submission_id": { "type": "string" },
    "year": { "type": "number" },
    "description": { "type": "string" },
    "amount_usd": { "type": "number" },
    "open": { "type": "boolean" },
    "created_at": { "type": "datetime" }
  },
  "indexed": ["submission_id", "year"],
  "_comment_purpose": "Past insurance claims for a business, one row per claim. The underwriting-review skill reads these to judge claim frequency and severity (e.g. a repeat fire, a large open claim) when recommending a workflow status."
}
